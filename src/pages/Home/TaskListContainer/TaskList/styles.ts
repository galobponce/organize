import styled from '@emotion/styled';
import { List } from '@chakra-ui/react';

export const TaskListWrapper = styled(List)`
  display: flex;
  align-items: center;
  flex-flow: column nowrap;
  margin-top: .5rem;
`;